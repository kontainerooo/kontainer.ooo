/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { KioCardFullWidthDirective } from './kio-card.directive';

describe('KioCardFullWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new KioCardFullWidthDirective();
    expect(directive).toBeTruthy();
  });
});
